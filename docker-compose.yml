services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: berkas-rm-backend
    restart: unless-stopped
    ports:
      - "3001:3000"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    env_file:
      - ./backend/.env
